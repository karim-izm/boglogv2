<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Set pitch black background -->
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>

        <!-- Use Roboto font for regular text -->
        <item name="android:fontFamily">@font/roboto</item>

        <!-- Use white buttons with black text -->
        <item name="colorButtonNormal">@android:color/white</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <!-- Style for buttons using "Beyond the Mountains" font -->
    <style name="CustomButton" parent="Widget.MaterialComponents.Button">
        <item name="android:backgroundTint">@android:color/white</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:fontFamily">@font/beyond_the_mountains</item>
    </style>

    <!-- Style for titles using "Beyond the Mountains" font -->
    <style name="TitleText" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:fontFamily">@font/beyond_the_mountains</item>
    </style>
</resources>
